{
  "name": "",
  "version": "1.0.0",
  "description": "lite-app init",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "lite-app --config ./app.json --env dev --target web",
    "build": "lite-app --config ./app.json --env prod --target app"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "liteapp-base": "^0.0.1",
    "liteapp-cli": "^0.0.1"
  }
}
